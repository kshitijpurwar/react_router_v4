import React from 'react'

export const Redirect = (props) => {
    return(
        <div>
            You were redirected to this page because old page does not exist anymore!!
        </div>
    )
}

export default Redirect